## Instructions

1. Clone the repo. (It is private, either login or use an access token)
2. run either `python main.py` or modify the bash script `bash run_pool.sh` and run it.
3. Add command line args as necessary to change path and other variables (use `python main.py --help`)
